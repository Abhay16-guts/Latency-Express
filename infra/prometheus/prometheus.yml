global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: 'latency-express-api'
    static_configs:
      - targets: ['api:4000', 'localhost:4000']
        labels:
          app: 'latency-express'
